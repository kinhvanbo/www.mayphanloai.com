@model IList<CategoryModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    <div class="category-grid home-page-category-grid">
        <div class="item-grid">
            @foreach (var item in Model)
            {
                <div class="item-box">
                    <div class="category-item">
                        <h2 class="title">
                            <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@Html.Raw(item.PictureModel.Title)">
                                @Html.Raw(item.Name)
                            </a>
                        </h2>
                        <div class="picture">
                            <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@Html.Raw(item.PictureModel.Title)">
                                <img alt="@Html.Raw(item.PictureModel.AlternateText)" src="@item.PictureModel.ImageUrl" title="@Html.Raw(item.PictureModel.Title)" />
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}